Option 1. Use the existing PostgreSQL 16 service. Create a dedicated login and database instead of using the superuser: connect as postgres with password <YOUR_PASSWORD>, run CREATE USER codejudge WITH PASSWORD 'codejudge'; CREATE DATABASE codejudge OWNER codejudge; then set appsettings.Development.json to Host=localhost;Port=5432;Database=codejudge;Username=codejudge;Password=codejudge and re-run Step 3. Do not commit the postgres superuser password anywhere. Record "verified locally against PostgreSQL 16; design targets 17" in the deviations table.