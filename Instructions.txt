First, commit and push the current state:
1. Run `dotnet build` and note whether it succeeds. Do not fix anything yet.
2. Make sure PROJECT_STATUS.md is at the repo root and README.md has, right under the title, this line:
   > **Status:** design complete; implementation in progress. See [PROJECT_STATUS.md](PROJECT_STATUS.md) for milestones and remaining work. Full design in `docs/`.
3. Open PROJECT_STATUS.md and correct the M5 checkboxes so they match what actually exists in src/CodeJudge.Api right now — tick only files that are present and compile.
4. `git add -A`, then commit with message:
   "feat: design docs, solution skeleton, domain/application/persistence layers, API in progress

   - System Design v1 and Database Design v1 in docs/
   - Domain aggregate with state machine, Application services and problem catalog
   - EF Core + Npgsql persistence with InitialCreate migration and claim query
   - SubmissionsController v1 and ApiResult envelope
   - PROJECT_STATUS.md tracks milestones and remaining work"
5. If no remote exists, tell me and stop; otherwise `git push -u origin main` (or the current branch) and confirm the push succeeded.

Then continue with the remaining work in PROJECT_STATUS.md, in this order, committing after each milestone with a conventional-commit message and ticking the corresponding checkboxes in PROJECT_STATUS.md as part of the same commit:

M5 (Api): UsersController, ProblemsController, ExceptionMiddleware, ValidationActionFilter, ApiKeyAuthenticationHandler, Swagger setup, Program.cs wiring, appsettings. Follow docs/System_Design_v1.md §6.7, §6.8, §6.10 and §10 exactly. Verify with `dotnet run` against local PostgreSQL that POST/GET return the ApiResult shape and that 400/401/404 carry the right errorCode. Commit: "feat(api): controllers, exception middleware, api-key auth, validation filter, swagger".

M6 (worker + evaluators): ProcessRunner and the two harness scripts first, then PythonEvaluator and JavaScriptEvaluator, then EvaluationSettings and EvaluationWorker, then CSharpEvaluator last. Follow System Design §6.4, §6.5 and Database Design §8. Verify a Python sum-two-numbers submission reaches status completed with test.testsPassed = 4. Commit: "feat(evaluation): background worker, harness protocol, python/js/c# evaluators".

M7 (tests): EvaluationServiceTests first (compile failure and correct solution — the two required by the brief), then SubmissionStateMachineTests, ProblemCatalogTests, validator tests, NetArchTest dependency rules, then the integration test with WebApplicationFactory + EF InMemory. Commit: "test: unit and integration tests for evaluation, domain, validation, architecture".

M8 (repo files): docker-compose.yml, Dockerfile, Postman collection, scripts/curl-samples.sh, docs/API_Documentation.md. Do NOT write the final README — leave the placeholder. Commit: "chore: docker, postman collection, curl samples, api documentation".

Rules: never deviate from the design docs without asking me first. Every time you make a decision that differs from the v1 docs (a new column, a renamed status, a changed default, a dropped feature), append a row to the "Deviations from v1 design" table in PROJECT_STATUS.md in the same commit — I need that table complete for the v2 documents. After each milestone, print the short git log and the updated deviations table.